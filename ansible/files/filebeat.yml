filebeat.prospectors:
- type: log
  enabled: true
  paths:
    - /var/log/earthrise.log
  multiline.pattern: '^\['
  multiline.negate: true
  multiline.match: after

output.logstash:
  hosts: ["cronos:5044"]
