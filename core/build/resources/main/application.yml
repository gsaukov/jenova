server:
  port: 8080

  #ssl:
    #key-store: classpath:keystore.p12
    #key-store-password: tomcat
    #keyStoreType: PKCS12
    #keyAlias: tomcat

management:
  address: 127.0.0.1
  port: 8081
  security:
    enabled: false

endpoints:
  shutdown:
    enabled: true