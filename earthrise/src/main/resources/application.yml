spring:
  application:
    name: earthrise

server:
  port: 8761

eureka:
  instance:
    hostname: 192.168.178.56
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  security:
    enabled: false