spring:
  application:
    name: earthrise
  cloud:
    inetutils:
      ignoredInterfaces:
        - lo

server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  security:
    enabled: false