spring:
  application:
    name: earthrise

server:
  port: 8761

  cloud:
    inetutils:
      preferredNetworks:
        - 192.168

eureka:
  server:
    enable-self-preservation: false
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://netrunner-01:${server.port}/eureka/


management:
  security:
    enabled: false