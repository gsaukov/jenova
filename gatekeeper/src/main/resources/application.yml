spring:
  application:
    name: gatekeeper

zuul:
  ignoredServices: '*'
  prefix: /jenova
  routes:
    user:
      path: /user/**
      serviceId: justitia
    authority:
      path: /authority/**
      serviceId: justitia
    echo:
      path: /echo/**
      serviceId: omnidrive

server:
  port: 8090
eureka:
  client:
    serviceUrl:
      defaultZone: http://deepwater:8761/eureka/

