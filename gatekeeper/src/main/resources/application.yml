spring:
  application:
    name: gatekeeper

zuul:
  ignoredServices: '*'
  prefix: /jenova
  routes:
    user:
      path: /user/**
      serviceId: omnidrive
      strip-prefix: false

ribbon:
  eager-load:
    enabled: true
  ReadTimeout: 3000
  ConnectTimeout: 1000

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000

server:
  port: 8090
eureka:
  client:
    serviceUrl:
      defaultZone: http://netrunner-01:8761/eureka/

management:
  security:
    enabled: false
