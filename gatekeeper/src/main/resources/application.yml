spring:
  application:
    name: gatekeeper

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8090

eureka:
  client:
    serviceUrl:
      defaultZone: http://deepwater:8761/eureka/

zuul:
  ignoredServices: '*'
  prefix: /jenova
  routes:
    justitia:
      path: /justitia-api/**
      sensitiveHeaders:
      serviceId: justitia
      stripPrefix: false
    omnidrive:
      path: /omnidrive-api/**
      sensitiveHeaders:
      serviceId: omnidrive
      stripPrefix: false