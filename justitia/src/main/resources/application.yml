spring:
  application:
    name: justitia

  datasource:
    url: jdbc:mysql://cronos:3306/justitia?useUnicode=true&serverTimezone=UTC
    username: dimitrios
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      minimumIdle: 5
      maximumPoolSize: 20
      idleTimeout: 30000
      maxLifetime: 2000000
      connectionTimeout: 30000

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true
    change-log: classpath:liquibase/changelog-master.xml

  cloud:
    refresh:
      enabled: false # FIXME: This needs to be removed once issue is fixed (bug with circular dependency).

server:
  port: 8002

eureka:
  client:
    serviceUrl:
      defaultZone: http://deepwater:8761/eureka/

---
spring:
  profiles: h2

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect